# Example .env for immich_faceless_album
# Copy this file to ".env" and fill in the real values (do NOT commit real secrets to git).
#
# Notes:
# - Set IMMICH_URL to the base URL of your Immich instance (include http(s) and port if non-default).
# - Use a valid API key for API_KEY.
# - For Gmail, prefer an App Password (not your account password) if using 2FA.
# - LOG_FILE_PATH should be a writable path on the machine running the script.

# --- Configuration ---
# URL of your Immich server (include protocol and port if needed)
IMMICH_URL=http://localhost:2283

# API key to authenticate with Immich
API_KEY=REPLACE_WITH_YOUR_API_KEY

# Pick a name for the album to sync into (example: "Things")
ALBUM_NAME=Things

# Path to log file used by the script (absolute path recommended)
LOG_FILE_PATH=/var/log/faceless_album_sync.log

# --- Email (optional) ---
# Set to "true" or "false" to enable sending emails on unhandled exceptions
ENABLE_EMAIL_ON_ERROR=false

# SMTP server and port (for Gmail: smtp.gmail.com and 465)
EMAIL_SMTP_SERVER=smtp.example.com
EMAIL_SMTP_PORT=465

# Email account username (sender)
EMAIL_USERNAME=your.email@example.com

# Email account password or app-specific password
EMAIL_PASSWORD=REPLACE_WITH_YOUR_EMAIL_APP_PASSWORD

# Recipient and sender addresses for error notifications
EMAIL_TO=recipient@example.com
EMAIL_FROM=your.email@example.com

# --- End of example ---
# Remove comments and rename to ".env" before running the application.
